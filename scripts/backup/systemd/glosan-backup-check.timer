[Unit]
Description=Weekly Glosan backup integrity check

[Timer]
# Sundays at 03:30 local time (after the nightly backup has run).
OnCalendar=Sun *-*-* 03:30:00
RandomizedDelaySec=30min
Persistent=true

[Install]
WantedBy=timers.target
